# 🏠 Dotfiles

> Набор конфигурационных файлов (dotfiles) для настройки рабочего окружения xfce + настройки базовых пакетов

Этот репозиторий содержит мои персонализированные настройки терминала, редакторов, утилит и других инструментов — всё, что делает работу в командной строке быстрой, удобной и красивой.

📁 Основные конфиги хранятся в папке [`home/`](./home/) — она зеркалирует структуру вашей домашней директории (`~`).

---

## ✨ Возможности

- Настройки LazyVim (LSP, автодополнение, Treesitter, темы)
- Настройки code-oss (конфиг, сниппеты)
- Настройки vscode (конфиг, сниппеты)
- Конфигурация шеллов: Bash, Zsh
- Настройки Git, LazyVim, Alacritty и др.

---

## Требования

- Требования
- git
- curl или wget
- bash/zsh
- kora-icon-theme для оформления

---

## ⚡ Быстрая установка

Запустите эту команду в терминале — **все файлы из `home/` будут скопированы в вашу домашнюю папку с перезаписью**:

```bash
curl -fsSL https://raw.githubusercontent.com/als-creator/dotfiles/main/install-dotfiles.sh | bash
```

Установить зависимости (если нужно):

```bash

# Ubuntu/Debian
sudo apt install git curl

# Arch
sudo pacman -S git curl

# macOS (с Homebrew)
brew install git
```

## 📂 Структура репозитория

dotfiles/
├── home/ # ← ваши будущие dotfiles (зеркало ~/)
│ ├── .bashrc
│ ├── .zshrc
│ ├── .config/
│ │ ├── nvim/
│ │ ├── kitty/
│ │ └── ...
│ └── ...
├── install-dotfiles.sh # универсальный установщик

Всё, что находится в home/, будет скопировано в ~ при установке.

## 🔐 Безопасность

Скрипт не отправляет данные, не скачивает бинарники, не использует sudo.
Он клонирует репозиторий во временную папку, копирует файлы и удаляет временные данные.
Перед установкой вы всегда можете прочитать сам скрипт.
Рекомендуется делать резервную копию важных конфигов перед установкой:

```bash
cp ~/.bashrc ~/.bashrc.bak
cp -r ~/.config/nvim ~/.config/nvim.bak
```

## ♻️ Обновление

Просто запустите установочный скрипт снова — он перезапишет старые файлы новыми.

## 🛠️ Ручная установка (альтернатива)

Если вы не доверяете автоматическим скриптам:

```bash
git clone https://github.com/als-creator/dotfiles.git ~/dotfiles
cp -rf ~/dotfiles/home/* ~/
rm -rf ~/dotfiles
```
